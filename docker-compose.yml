version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    container_name: audio-interviewer-frontend
    networks:
      - app-network

networks:
  app-network:
    external: true
